# Copy to backend/.env and set real values. Do not commit backend/.env.

# Database (use postgres:5432 when running in Docker)
DATABASE_URL=postgresql+asyncpg://moveo:your_password@localhost:5432/moveo_crypto

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_MINUTES=1440

# Optional API keys (fallbacks used when empty)
CRYPTOPANIC_API_KEY=
OPENROUTER_API_KEY=
HUGGINGFACE_API_KEY=
