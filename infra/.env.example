
# Run from infra/:  docker compose up

# Postgres (used by postgres service)
POSTGRES_USER=moveo
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=moveo_crypto

# Backend: DB URL when running in Docker (host = postgres, not localhost)
DATABASE_URL=postgresql+asyncpg://moveo:change_me_in_production@postgres:5432/moveo_crypto

# Frontend: backend API URL (for Vite dev server / client)
VITE_API_BASE_URL=http://localhost:8000
